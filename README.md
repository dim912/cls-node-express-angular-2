# cls-node-express-angular-2


```sh
$ npm install
$ node index.js
$ node index.js => to run nodejs http module web server
$ node app.js => to run nodejs express application(non route)
$ node server.js => to run express modular app using express 4 routes

views => jade 

```